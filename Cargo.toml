[package]
name = "tading-platform"
version = "0.1.0"
edition = "2024"
authors = ["Yash"]
description = "A production-grade trading ecosystem built from scratch"

[dependencies]

serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
clap = { version = "4.4", features = ["derive"] }
thiserror = "1.0"
uuid = { version = "1.6", features = ["v4", "serde"] }
sha2 = "0.10"
chrono = { version = "0.4", features = ["serde"] }
serde_json = "1.0"